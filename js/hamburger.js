document.addEventListener('DOMContentLoaded', function () {
    console.log('ok');

    const hamburgerBtn = document.querySelector('.header__mobile-menu')

    hamburgerBtn.addEventListener('click', () => {
        console.log('click');

    })
})